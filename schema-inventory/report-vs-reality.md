# Cybernara Report-vs-Reality Reconciliation

This document reconciles the claims made in the prior repository analysis report against the verified, live Supabase PostgreSQL database state.

| Claim | Verified Status | Evidence (Query / Script Results) |
|---|---|---|
| 58 tables with RLS policies, 21 indexes, 65k+ requirements rows, 81k+ mapping rows. | **Partially True** | **Tables**: 147 tables exist and all 147 have RLS enabled (verified by `node scripts/schema-audit.mjs`).<br>**Indexes**: 127 indexes exist (verified by `schema-audit.mjs`).<br>**Requirements**: `3,643` rows in `framework_requirements` (not 65k+).<br>**Mappings**: `4,523` rows in `control_mappings` (not 81k+). |
| G-01, G-02, G-03, G-04, G-10 are completely closed. | **Verified True** | **G-01**: Execution graph tables exist, constraints are not-null, read-path cutover is complete, and frontend sections are implemented and tested (Playwright E2E passes).<br>**G-02**: `findings.assessment_item_id` FK is verified.<br>**G-03**: `findings.assessment_item_id`/`test_result_id` are nullable with `findings_has_source` CHECK constraint, and `risk_acceptances.risk_id` is linked.<br>**G-04**: `report_exports` contains `artifact_bytes` linked to `assessment_snapshots`.<br>**G-10**: All repositories use `TenantScopedDb` under `app_runtime` context, and RLS is forced. |
| G-05 through G-14 have target-state tables created but require Backfill/Constrain/Cutover/Contract/Frontend. | **Verified True** | **G-05**: Expand complete, backfill is pending (failed).<br>**G-06**: Phase 1 Expand complete, Phase 2 Expand / Backfill & Cutover / Frontend drawer pending.<br>**G-07**: Expand complete, Backfill / Cutover / Frontend UI pending.<br>**G-08**: Expand complete, Backfill / Cutover / Frontend UI pending.<br>**G-09**: Phase 1 Expand complete, Phase 2 Expand / Cutover / Frontend UI pending.<br>**G-11**: Expand and Cutover complete, Frontend UI pending.<br>**G-12**: Expand and Cutover complete, Frontend UI pending.<br>**G-13**: Expand and Cutover complete, Frontend UI pending.<br>**G-14**: RLS tooling drift guard complete, other design audits pending. |
| `assessment_items` has 960+ rows with `control_instance_id IS NULL`. | **Verified False** | The G-01 backfill was successfully run, and then the Constrain stage was completed by setting `control_instance_id NOT NULL` and `question_version_id NOT NULL` in migration `0026_g01_constrain_execution_graph_not_null.sql`. There are currently **0** rows with `control_instance_id IS NULL` because the constraint is enforced. |
